<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android">

    <View android:id="@id/player_dark_bg"
          android:layout_width="match_parent"
          android:layout_height="match_parent"
          android:background="@android:color/black"/>

    <com.google.android.exoplayer2.ui.AspectRatioFrameLayout
        android:id="@id/player_content_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center">

        <!-- Video surface will be inserted as the first child of the content frame. -->

        <View android:id="@id/player_shutter"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="@android:color/black"/>

    </com.google.android.exoplayer2.ui.AspectRatioFrameLayout>

    <!-- This will be replaced by the controller view -->
    <View
        android:id="@id/player_controller_placeholder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <!-- Here comes the preview image (and the play icon) -->
    <FrameLayout
        android:id="@id/player_preview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</merge>